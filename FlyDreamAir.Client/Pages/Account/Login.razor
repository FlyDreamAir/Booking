@page "/Account/Login"

<!-- Fill the background as in the design -->
<div style="width: 100%; height: 100%; background-color: var(--mud-palette-surface); position: fixed; z-index: -1;">
</div>

<DeviceView>
    <Desktop>
        <MudText Typo="@Typo.h3"
                 Align="@Align.Center"
                 Color="@Color.Primary"
                 Style="user-select: none;">
            Sign in
        </MudText>

        <div class="m-2">
            <div style="max-width: 50%; margin-left: auto; margin-right: auto;">
                <LoginView />
            </div>
        </div>
    </Desktop>
    <Mobile>
        <div class="m-2">
            <MudImage Src="assets/plane.png" ObjectFit="@ObjectFit.Cover"
                      Style="max-width: 100%; margin-left: auto; margin-right: auto; display: block;" />
        </div>
        <MudText Typo="@Typo.h4"
                 Align="@Align.Center"
                 Color="@Color.Primary"
                 Style="user-select: none;">
            <b>Find your flight</b>
        </MudText>

        <div class="m-2">
            <div style="margin-left: auto; margin-right: auto;">
                <LoginView />
            </div>
        </div>
    </Mobile>
</DeviceView>
